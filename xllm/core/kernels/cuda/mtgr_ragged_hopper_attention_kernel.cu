// Umbrella TU for Hopper ragged attention code.
// Keep the implementation physically split by role while preserving the
// existing single-file compile contract used by local extension builds.
#include "mtgr_ragged_hopper_attention_common.cu"
#include "mtgr_ragged_hopper_attention_unified_entry.cu"

}  // namespace xllm::kernel::cuda
